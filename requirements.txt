# Core dependencies for audio transcription
sounddevice>=0.4.6
numpy>=1.21.0

# Audio file support (choose one option)
librosa>=0.9.0
# or
# soundfile>=0.12.0

# Faster Whisper (default model family)
faster-whisper>=0.10.0

# NVIDIA Parakeet v3 (optional model)
torch>=2.0.0
torchaudio>=2.0.0

# NVIDIA NeMo Toolkit for Parakeet v3
nemo_toolkit[asr]>=2.4.0

# Optional - improves CUDA performance for Parakeet v3
cuda-python>=12.3

# Optional two-speaker diarization (CPU, no HF token)
diarize>=0.1.1

# GUI (optional, for dialogs)
# tkinter is built into Python
